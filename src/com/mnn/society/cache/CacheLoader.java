package com.mnn.society.cache;

import java.awt.image.BufferedImage;
import java.io.BufferedInputStream;
import java.io.File;
import java.io.FileInputStream;
import java.io.IOException;
import java.io.InputStream;
import javax.imageio.ImageIO;

import com.mnn.society.server.utils.Logger;

public class CacheLoader {
	public CacheLoader () {
		
	}
	
	public Cache loadGridCacheFromFile (String filename) {
		//File file = new File(ref);
		
		
		return null;
	}
	
	public Cache loadGridCacheFromServer () {
		return null;
	}
	
	/*
    Logger.log(Logger.LOG_CLIENT, file.getAbsolutePath());
      if (file == null) {
          throw new IOException("Cannot find: "+ref);
      }
     
     
     InputStream is=null;
     
     try {
             is = new FileInputStream(file);
             
     } catch (IOException e) {
         e.toString();
     }
    
      
      
     */
     
     //BufferedImage bufferedImage = ImageIO.read(new BufferedInputStream(is)); 
     
     //return bufferedImage;
}